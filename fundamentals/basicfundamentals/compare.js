// alert( 2 > 1 );
// alert(2 == 1);
// alert (2 != 1);

//ASSIGNING A COMPARISON TO A VARIABLE
// let result = 5 > 4;
// alert(result);

//COMPARING STRINGS
// alert('Z' > 'A'); // TRUE
// alert('Glow' > 'Glee'); // true , because o comes after e.
// alert('Bee' > 'Be'); // true because bee is a longer string.

//WHEN COMPARING VALUES OF DIFFERENT TYPES, STRINGS BECOME NUMBERS.
// alert('2' > 1); // true, 2 becomes a number.
// alert('01' == 1); //true, '01' becomes a number.

// USING STRICT EQUALS === COMPARES OPERATOR TYPES (TYPEOF) VS. THE VALUES AND INSTANT CONVERSION.
//STRICT CHECK
alert(null === undefined); //false

//NON-STRICT CHECK
alert(null == undefined); //true

//IN MATHS NULL BECOMES 0 AND UNDEFINED BECOMES NaN.

